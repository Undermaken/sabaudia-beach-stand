import { createTheme } from "@mantine/core";

/**
 * Mantine theme. Extend colors, fonts, and component defaults here.
 * Mobile-first: the default spacing/typography scale targets small screens.
 */
export const theme = createTheme({
  primaryColor: "blue"
});
